# 产品需求文档 — 求职助手 JobTracker

## 产品概述

JobTracker 是一款面向求职者的全流程投递管理工具，将分散的投递进度、JD 分析、面经记录整合为单一工作台，并引入 AI 辅助面试准备，解决求职过程中信息碎片化、面试前准备不足的核心痛点。

---

## 市场背景与用户洞察

### 目标用户
- **核心用户**：参加秋招 / 春招的应届毕业生，同时投递 20-50+ 家公司
- **延伸用户**：社招跳槽者、实习申请者

### 用户痛点
1. **信息碎片化**：投递记录散落在 Excel、聊天记录、备忘录中，状态难以追踪
2. **JD 信息密度低**：原始职位描述冗长杂乱，难以快速提取关键要求
3. **面试准备低效**：面试前需要反复翻阅公司官网、JD、过往面经，缺少一站式聚合
4. **面经沉淀弱**：每次面试的题目和回答缺乏系统记录，无法复盘迭代

### 竞品差异化
| 维度 | Excel / 飞书表格 | Notion 模板 | JobTracker |
|------|------------------|-------------|------------|
| 学习成本 | 低 | 中 | 低 |
| 状态流转 | 手动改单元格 | 手动改属性 | 一键切换 + 可视化徽章 |
| JD 处理 | 纯文本粘贴 | 纯文本粘贴 | AI 结构化 + Markdown 渲染 |
| 面试建议 | 无 | 无 | AI 结合简历 × JD 生成 |
| 面经管理 | 无结构 | 需手动设计 | 内建场次-题目模型 |
| 数据安全 | 云端 | 云端 | 纯本地存储，零泄露风险 |

---

## 核心用户旅程

### 旅程 1：管理投递全流程
1. 用户通过「+ 添加投递」录入公司名、岗位、城市、投递日期
2. 粘贴 JD 原文 → 点击「AI 格式化」→ 自动生成结构化 Markdown（职位概述 / 岗位职责 / 任职要求 / 加分项 / 薪资福利）
3. 在列表页通过状态标签一键切换进度：待投递 → 已投递待回复 → 待笔试 → 笔试完待通知 → 面试中 → 已 Offer / 已挂
4. 支持按状态、面试轮次、公司名搜索筛选；超过 7 天未跟进自动高亮提醒

### 旅程 2：面试前高效备战
1. 列表页点击「面试前浏览」进入聚合看板
2. 单页展示：公司简介 + 结构化 JD + AI 面试建议 + 历次面经
3. 开启「自测模式」可隐藏参考答案，逐题模拟真实问答
4. AI 面试建议基于用户简历与 JD 的匹配度分析，点明核心匹配点、潜在短板、应强调的亮点、可能被问的问题方向

### 旅程 3：面试后沉淀面经
1. 进入公司详情 → 面经 Tab → 添加面试记录
2. 填写日期、轮次（一面 ~ 六面 / HR 面 / 自定义），逐题录入题目 + 回答
3. 每题可单独调用 AI 生成参考答案，对比复盘
4. 支持面经导出为 TXT 文件，方便分享或打印

### 旅程 4：配置与数据管理
1. 设置页配置 API Key（DeepSeek / 千问），Key 仅存本地浏览器
2. 上传简历（PDF / .docx）→ 客户端提取文本，供 AI 分析使用
3. 支持数据导出为 JSON 备份，导入恢复，换设备无忧

---

## 功能规格

### 投递状态机
预设 7 个状态，覆盖求职全生命周期，支持用户自定义扩展：

```
待投递 ──→ 已投递待回复 ──→ 待笔试 ──→ 笔试完待通知 ──→ 面试中 ──→ 已 Offer
                                                    ↓              ↓
                                                  已挂 ←─────────── ✕
```

### AI 能力矩阵
| 能力 | 触发场景 | 依赖 | 模型策略 |
|------|----------|------|----------|
| JD 格式化 | 添加 / 编辑投递 | 任一 API Key | 文本：DeepSeek 优先，千问降级 |
| JD 截图识别 | 添加投递时上传截图 | 任一 API Key | 图片：千问 VL 优先，DeepSeek 降级 |
| 面试建议 | 详情页「AI 分析」 | API Key + 简历 + 已格式化的 JD | 文本模型 |
| 面经参考答案 | 面试记录中单题「AI 生成」 | API Key | 文本模型 |

### 数据模型
```typescript
Application {
  id: string               // UUID v4
  companyName: string      // 公司名（必填）
  position: string         // 岗位（必填）
  applicationDate: string  // 投递日期 YYYY-MM-DD
  status: string           // 当前状态（预设或自定义）
  interviewRound?: string  // 当前面试轮次
  workCity?: string        // 工作城市
  companyBrief?: string    // 公司简介
  notes?: string           // 备注（内推人、HR 联系方式等）
  offerSalary?: string     // Offer 薪资（已 Offer 时可见）
  jdRaw?: string           // JD 原文
  jdFormatted?: string     // AI 格式化后的 Markdown
  aiAnalysis?: string      // AI 面试建议 Markdown
  interviews: Interview[]  // 面经列表
  createdAt: string        // 创建时间 ISO
  updatedAt: string        // 最后更新时间 ISO
}

Interview {
  id: string
  date: string             // 面试日期 YYYY-MM-DD
  round: string            // 轮次
  notes?: string           // 本场备注
  questions: Question[]
}

Question {
  id: string
  question: string         // 面试题目
  answer?: string          // 我的回答
  refAnswer?: string       // AI 参考答案
}
```

---

## 非功能需求

| 类别 | 决策 |
|------|------|
| 部署 | 纯静态 HTML，零服务器，GitHub Pages 托管 |
| 存储 | localStorage，数据完全归用户所有 |
| 隐私 | API Key 仅存本地，不经过任何中间服务器 |
| 兼容 | Chrome / Edge / Firefox 现代版本 |
| 性能 | 首次加载 < 2s（CDN 依赖），操作响应 < 100ms |
| 可维护性 | 单文件架构，无构建步骤，修改即生效 |

---

## 版本路线

### v1.0（已完成）
- 投递 CRUD + 状态流转 + 搜索筛选
- JD AI 格式化 + 截图 OCR 识别
- AI 面试建议（简历 × JD 匹配分析）
- 面经记录 + AI 参考答案生成
- 面试前浏览 + 自测模式
- 简历上传 + PDF/Word 文本提取
- 数据导出 / 导入
- 自定义状态标签

### v2.0（规划中）
- [ ] 投递时间线可视化（甘特图 / 瀑布流）
- [ ] 面试邀请邮件解析（一键导入面试时间）
- [ ] 多设备同步（可选 WebDAV / GitHub Gist）
- [ ] 统计分析面板（投递转化率、面试通过率、平均响应天数）
- [ ] PWA 支持（可安装到桌面，离线使用）
